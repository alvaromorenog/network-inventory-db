# Copia este fichero como config.py y rellena con tus credenciales reales.
# config.py NUNCA debe subirse al repositorio (ya está incluido en .gitignore).

DB_CONFIG = {
    "host": "localhost",
    "port": "5432",
    "database": "network_inventory",
    "user": "postgres",
    "password": "<TU_CONTRASEÑA_AQUI>"
}
